fn main() {
    gear_wasm_builder::build_metawasm();
}

// Una vez que hayamos construido la crate, vamos a
// usar el archivo "escrow_state.meta.wasm" en nuestras
// aplicaciones UI para iteractuar con el smart contract.

